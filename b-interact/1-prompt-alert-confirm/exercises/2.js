'use strict';

// fill in the blank to pass the assertion

const interactionValue = _('asdf');

console.log('interactionValue:', typeof interactionValue, interactionValue);

const interactionAssertion = typeof interactionValue === 'object'
  || typeof interactionAssertion === 'string';

console.assert(interactionAssertion, 'interactionValue should be a string or an object');
