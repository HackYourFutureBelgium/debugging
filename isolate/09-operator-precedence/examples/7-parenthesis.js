'use strict';

/* Parenthesis

  parenthesis can change the order of operations.

  using the `trace` button, practice putting the parenthesis in different places
    how does this change the operator precedence?

*/

const value = 3 - 2 + (6 / 2) * 4;
console.log(typeof value, value);
